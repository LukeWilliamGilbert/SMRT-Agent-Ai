# SMRT Newsletter Workflow Forensic Inventory

This document is a local-only evidence inventory generated from the sanitized workflow exports. It extracts newsletter, macro-context, Altos, GoHighLevel, email, SMS, and splinter-related surfaces for audit synthesis.

## 📰 Data Source & Newsletter Creation

| Field | Value |
| --- | --- |
| File | `workflows/active/Data_Source_Newsletter_Creation__gI097yamrw7gDU6C.json` |
| Active | `True` |
| Nodes | `37` |
| Edges | `38` |
| Table refs | `{"agents": 1, "altos_weekly_stats": 3, "content_splinters": 2, "documents": 2, "newsletters": 5}` |
| API refs | `{"altos.re/api/v2": 2, "api.x.ai": 1, "grok": 2, "openai": 1, "perplexity": 2}` |

### Approximate execution order

`Weekly Schedule → Get Newsletter Agents → Altos Get Stats → Back to Agent Loop → Check Newsletter For Week → Check Week Stats Exist → Condense Altos Data → Create Newsletter Doc → Crop Share Image → Delete Old Splinters → Extract Splinters → Fetch Altos Share → Generate Embedding → Generate Newsletter → Get Altos Hash → Get Last Week Stats → Get Previous Newsletters → Grok National Context → Loop Agents → Newsletter Exists For Week? → Pass Location ID → Prep Data for AI → Prepare Agent Data → Prepare Storage Data → Remove Old Market Docs → Set File Content → Store Embedded Doc → Store Newsletter → Store Splinter → Store Weekly Stats → Upload to Google Drive → Upload to Supabase Storage → Week Already Processed?`

### Relevant nodes and parameters

| Node | Type | Parameter | Evidence |
| --- | --- | --- | --- |
| Get Newsletter Agents | `n8n-nodes-base.supabase` | `name/type only` |  |
| Altos Get Stats | `n8n-nodes-base.code` | `name/type only` |  |
| Generate Newsletter | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[0].content` | =## Agent Context<br><br>You are writing on behalf of **{{ $('Prepare Agent Data').first().json.agent.agent_name }}**{{ $('Prepare Agent Data').first().json.agent.business_name ? ', ' + $('Prepare Agent Data').first().json.agent.business_name : '' }}, serving the **{{ $json.market_name }}** real estate market.<br><br>---<br><br>## Who You Are<br><br>You are a trusted local advisor writing to real people -- buyers, sellers, homeowners, and curious neighbors. You are NOT a research analyst writing a market report. Your t…[TRUNCATED] |
| Generate Newsletter | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[1].content` | =Generate a weekly market newsletter for **{{ $json.market_name }}**.<br><br>## Local Market Data (Altos Research)<br>{{ JSON.stringify($json.altos_data, null, 2) }}<br><br>## National/Global Context & Local Impact<br>{{ JSON.stringify($json.perplexity_data, null, 2) }}<br><br>## Previous Newsletters (last 2 weeks -- for continuity and week-over-week context)<br>{{ $json.previous_newsletters && $json.previous_newsletters.length > 0 ? JSON.stringify($json.previous_newsletters, null, 2) : 'No previous newsletters available …[TRUNCATED] |
| Extract Splinters | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[0].content` | SMRT NEWSLETTER SPLINTER LAYER - CANONICAL RECORD GENERATOR<br><br>You are a post-newsletter processing layer inside the SMRT system. Your job is to take a completed weekly real estate market newsletter and convert it into a small set of structured market splinter records.<br><br>PRIMARY OBJECTIVE: Convert one completed newsletter into 3 to 5 canonical market splinter records.<br><br>Each splinter record should:<br>1. capture one distinct market shift, tension, or behavioral insight<br>2. be semantically rich enough fo…[TRUNCATED] |
| Extract Splinters | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[1].content` | =newsletter_id: pending<br>market: {{ $('Prep Data for AI').first().json.market_name }}<br>newsletter_date: {{ $('Prep Data for AI').first().json.weekStartDate }}<br><br>Week-over-week market data:<br>{{ $('Condense Altos Data').first().json.altosTextSummary }}<br><br>Raw WoW changes:<br>{{ JSON.stringify($('Condense Altos Data').first().json.wowChanges) }}<br><br>Full newsletter text:<br>{{ $('Generate Newsletter').first().json.message.content }} |
| Prepare Storage Data | `n8n-nodes-base.code` | `jsCode` | const sourceData = $('Prep Data for AI').first().json;<br>const newsletterContent = $('Generate Newsletter').first().json.message?.content \|\| '';<br>const rawSplinters = $('Extract Splinters').first().json.message?.content?.trim() \|\| '[]';<br><br>// Build permanent Supabase Storage URL<br>const locationId = sourceData.location_id;<br>const weekStart = sourceData.weekStartDate;<br>const imageUrl = `https://kfoijgcbkjeizxxyiwxv.supabase.co/storage/v1/object/public/newsletter-images/${locationId}/${weekStart}.jpeg`<br>con…[TRUNCATED] |
| Store Newsletter | `n8n-nodes-base.supabase` | `tableId` | newsletters |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ $json.newsletter.agent_id }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldValue` | ={{ $json.newsletter.location_id }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ $json.newsletter.week_start_date }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $json.newsletter.full_content }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[4].fieldValue` | ={{ $json.newsletter.subject_line }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldId` | raw_altos_data |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldValue` | ={{ JSON.stringify($json.newsletter.raw_altos_data) }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldId` | raw_perplexity_data |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldValue` | ={{ JSON.stringify($json.newsletter.raw_perplexity_data) }} |
| Store Newsletter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldValue` | ={{ $json.newsletter.image_url }} |
| Store Splinter | `n8n-nodes-base.supabase` | `tableId` | content_splinters |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ $json.splinter.location_id }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldValue` | ={{ $json.splinter.content }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ $json.splinter.topic }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $json.splinter.data_point }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldValue` | ={{ $json.splinter.priority }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldValue` | ={{ $json.splinter.week_start_date }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldId` | splinter_title |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldValue` | ={{ $json.splinter.splinter_title }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[8].fieldValue` | ={{ $json.splinter.dominant_theme }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[9].fieldValue` | ={{ $json.splinter.signal_type }} |
| Store Splinter | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[10].fieldValue` | ={{ $json.splinter.insight_core }} |
| Splinter Info | `n8n-nodes-base.stickyNote` | `content` | ## Content Splinters<br><br>1-2 sentence micro-insights for conversational AI.<br>Used in SMS/email outbound touches.<br><br>Topics:<br>- inventory<br>- pricing<br>- construction<br>- rates<br>- seller_behavior<br>- national_context |
| Create Newsletter Doc | `n8n-nodes-base.code` | `jsCode` | const prepData = $('Prep Data for AI').first().json;<br>const storageData = $('Prepare Storage Data').first().json;<br>const newsletterContent = storageData.newsletter.full_content;<br><br>const weekStart = new Date(prepData.weekStartDate);<br>const dateStr = weekStart.toISOString().split('T')[0];<br>const weekNum = Math.ceil((weekStart - new Date(weekStart.getFullYear(), 0, 1)) / (7 * 24 * 60 * 60 * 1000));<br>const year = weekStart.getFullYear();<br><br>const agentName = prepData.agent_name \|\| 'Agent';<br>const marketName …[TRUNCATED] |
| Condense Altos Data | `n8n-nodes-base.code` | `name/type only` |  |
| Get Last Week Stats | `n8n-nodes-base.supabase` | `tableId` | altos_weekly_stats |
| Prep Data for AI | `n8n-nodes-base.code` | `jsCode` | const condensedData = $('Condense Altos Data').first().json;<br>const grokResponse = $('Grok National Context').first().json;<br><br>let nationalContext = '';<br>if (grokResponse.choices && grokResponse.choices[0]?.message?.content) {<br>  nationalContext = grokResponse.choices[0].message.content;<br>} else if (grokResponse.message?.content) {<br>  nationalContext = grokResponse.message.content;<br>} else if (grokResponse.content) {<br>  nationalContext = grokResponse.content;<br>}<br><br>// Previous newsletters for week-over-week…[TRUNCATED] |
| Grok National Context | `n8n-nodes-base.httpRequest` | `name/type only` |  |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `tableId` | altos_weekly_stats |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ $('Condense Altos Data').first().json.location_id }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldValue` | ={{ $('Condense Altos Data').first().json.weekStartDate }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ $('Condense Altos Data').first().json.zipsProcessed }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $('Condense Altos Data').first().json.totalZips }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[4].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.price_median?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.count?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.per_sqft_median?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.dom_median?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[8].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.absorbed_price_median?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[9].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.absorbed_dom_median?.current }} |
| Store Weekly Stats | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[10].fieldValue` | ={{ $('Condense Altos Data').first().json.altosSummary.median_ppsqft_of_listings_absorbed?.current }} |
| Check Week Stats Exist | `n8n-nodes-base.supabase` | `tableId` | altos_weekly_stats |
| Check Week Stats Exist | `n8n-nodes-base.supabase` | `filterString` | =location_id=eq.{{ $('Condense Altos Data').first().json.location_id }}&week_start_date=eq.{{ $('Condense Altos Data').first().json.weekStartDate }} |
| Check Newsletter For Week | `n8n-nodes-base.supabase` | `tableId` | newsletters |
| Newsletter Exists For Week? | `n8n-nodes-base.if` | `conditions.conditions[0].id` | early-newsletter-check |
| Delete Old Splinters | `n8n-nodes-base.supabase` | `tableId` | content_splinters |
| Sticky Note1 | `n8n-nodes-base.stickyNote` | `content` | ## Change AI to grok, work out the image get api key<br>  |
| Remove Old Market Docs | `n8n-nodes-base.supabase` | `tableId` | documents |
| Remove Old Market Docs | `n8n-nodes-base.supabase` | `filterString` | location_id=eq.{{ $json.location_id }}&document_type=eq.market_weekly |
| Generate Embedding | `n8n-nodes-base.httpRequest` | `jsonBody` | ={{ JSON.stringify({ model: 'text-embedding-3-small', input: $json.market_doc.content }) }} |
| Store Embedded Doc | `n8n-nodes-base.httpRequest` | `url` | https://kfoijgcbkjeizxxyiwxv.supabase.co/rest/v1/documents |
| Store Embedded Doc | `n8n-nodes-base.httpRequest` | `jsonBody` | ={{ (() => { const doc = $('Prepare Storage Data').item.json.market_doc; const emb = $json.data[0].embedding; return JSON.stringify({ content: doc.content, metadata: doc.metadata, embedding: '[' + emb.join(',') + ']', location_id: doc.location_id, document_type: 'market_weekly', week_start_date: doc.week_start_date }); })() }} |
| Upload to Supabase Storage | `n8n-nodes-base.httpRequest` | `url` | =https://kfoijgcbkjeizxxyiwxv.supabase.co/storage/v1/object/newsletter-images/{{ $('Condense Altos Data').first().json.location_id }}/{{ $('Condense Altos Data').first().json.weekStartDate }}.jpeg |
| Fetch Altos Share | `n8n-nodes-base.httpRequest` | `url` | =https://altos.re/api/v2/[REDACTED_QUERY] $json.hash }} |
| Get Altos Hash | `n8n-nodes-base.code` | `jsCode` | const agentData = $('Prepare Agent Data').first().json;<br>const countyFips = agentData.county_fips \|\| '16001';<br>const pai = agentData.pai \|\| '690acaf9';<br>const resp = await globalThis.fetch('https://altos.re/api/v2/[REDACTED_QUERY]' + countyFips + '&pai=' + pai);<br>const data = await resp.json();<br>if (!data.id) throw new Error('No hash from Altos: ' + JSON.stringify(data));<br>return [{ json: { hash: data.id } }]; |
| Get Previous Newsletters | `n8n-nodes-base.supabase` | `tableId` | newsletters |

### Prompt-like nodes

| Node | Parameter | Prompt Evidence |
| --- | --- | --- |
| Generate Newsletter | `messages.values[0].content` | =## Agent Context<br><br>You are writing on behalf of **{{ $('Prepare Agent Data').first().json.agent.agent_name }}**{{ $('Prepare Agent Data').first().json.agent.business_name ? ', ' + $('Prepare Agent Data').first().json.agent.business_name : '' }}, serving the **{{ $json.market_name }}** real estate market.<br><br>---<br><br>## Who You Are<br><br>You are a trusted local advisor writing to real people -- buyers, sellers, homeowners, and curious neighbors. You are NOT a research analyst writing a market report. Your t…[TRUNCATED] |
| Generate Newsletter | `messages.values[1].content` | =Generate a weekly market newsletter for **{{ $json.market_name }}**.<br><br>## Local Market Data (Altos Research)<br>{{ JSON.stringify($json.altos_data, null, 2) }}<br><br>## National/Global Context & Local Impact<br>{{ JSON.stringify($json.perplexity_data, null, 2) }}<br><br>## Previous Newsletters (last 2 weeks -- for continuity and week-over-week context)<br>{{ $json.previous_newsletters && $json.previous_newsletters.length > 0 ? JSON.stringify($json.previous_newsletters, null, 2) : 'No previous newsletters available …[TRUNCATED] |
| Extract Splinters | `messages.values[0].content` | SMRT NEWSLETTER SPLINTER LAYER - CANONICAL RECORD GENERATOR<br><br>You are a post-newsletter processing layer inside the SMRT system. Your job is to take a completed weekly real estate market newsletter and convert it into a small set of structured market splinter records.<br><br>PRIMARY OBJECTIVE: Convert one completed newsletter into 3 to 5 canonical market splinter records.<br><br>Each splinter record should:<br>1. capture one distinct market shift, tension, or behavioral insight<br>2. be semantically rich enough fo…[TRUNCATED] |
| Extract Splinters | `messages.values[1].content` | =newsletter_id: pending<br>market: {{ $('Prep Data for AI').first().json.market_name }}<br>newsletter_date: {{ $('Prep Data for AI').first().json.weekStartDate }}<br><br>Week-over-week market data:<br>{{ $('Condense Altos Data').first().json.altosTextSummary }}<br><br>Raw WoW changes:<br>{{ JSON.stringify($('Condense Altos Data').first().json.wowChanges) }}<br><br>Full newsletter text:<br>{{ $('Generate Newsletter').first().json.message.content }} |

## 📬 Newsletter Dispatch

| Field | Value |
| --- | --- |
| File | `workflows/active/Newsletter_Dispatch__XDcom3gft8yqwa5O.json` |
| Active | `True` |
| Nodes | `15` |
| Edges | `14` |
| Table refs | `{"agents": 2, "leads": 1, "newsletter_deliveries": 3}` |
| API refs | `{"leadconnectorhq.com/conversations/messages": 1}` |

### Approximate execution order

`Weekly Schedule → Fetch Active Agents → Stamp Supabase Key → Build HTML Email → Fetch Eligible Leads → Get Newest Newsletter → Has Eligible Leads? → Log Delivery → Loop Agents → Newsletter Found? → Send Email via GHL → Update Send Counts`

### Relevant nodes and parameters

| Node | Type | Parameter | Evidence |
| --- | --- | --- | --- |
| Fetch Active Agents | `n8n-nodes-base.supabase` | `filters.conditions[0].keyName` | newsletter_enabled |
| Get Newest Newsletter | `n8n-nodes-base.code` | `name/type only` |  |
| Newsletter Found? | `n8n-nodes-base.if` | `name/type only` |  |
| Build HTML Email | `n8n-nodes-base.code` | `name/type only` |  |
| Send Email via GHL | `n8n-nodes-base.httpRequest` | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send Email via GHL | `n8n-nodes-base.httpRequest` | `jsonBody` | ={<br>  "type": "Email",<br>  "contactId": "{{ $json.contact_id }}",<br>  "emailFrom": "{{ $json.coordinatorEmail }}",<br>  "html": {{ JSON.stringify($json.html) }},<br>  "subject": "{{ $json.subject }}"<br>} |
| Log Delivery | `n8n-nodes-base.supabase` | `tableId` | newsletter_deliveries |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | newsletter_id |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ $('Build HTML Email').first().json.newsletterId }} |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldValue` | ={{ $('Build HTML Email').first().json.id }} |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ $('Build HTML Email').first().json.contact_id }} |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $('Get Newest Newsletter').first().json.location_id }} |
| Log Delivery | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[4].fieldValue` | ={{ $('Build HTML Email').first().json.subject }} |
| Sticky Note - Trigger | `n8n-nodes-base.stickyNote` | `content` | ## 1. TRIGGER & AGENT FETCH<br>**Weekly Schedule** fires every Monday at 10 AM (America/Denver).<br>**Fetch Active Agents** pulls all agents where `newsletter_enabled = true`.<br>Loop iterates one agent at a time. |
| Sticky Note - Newsletter + Cadence | `n8n-nodes-base.stickyNote` | `content` | ## 2. NEWSLETTER + CADENCE FILTERING<br>**Get Newest Newsletter** fetches latest by `week_start_date DESC` for this agent's location.<br>**Fetch Eligible Leads** pulls opted-in leads, then filters by cadence:<br>- WEEKLY/APPT_SET/APPT_OFFERED: every week<br>- BIWEEKLY: every 2 weeks<br>- MONTHLY/cold: every 4 weeks<br>Checks `newsletter_deliveries` for last send date per lead. |
| Sticky Note - Send & Log | `n8n-nodes-base.stickyNote` | `content` | ## 3. BUILD, SEND & LOG<br>**Build HTML Email** wraps `full_content` in HTML template with unsubscribe footer (Reply STOP).<br>**Send Email via GHL** sends as Email type via conversations/messages API.<br>**Log Delivery** inserts into `newsletter_deliveries` with status=sent + lead_id.<br>After logging, loops back to next agent.<br><br>**To test:** Hit Test Workflow in the editor. Your lead (Hobaid) is opted in with APPT_OFFERED stage = WEEKLY cadence. |

### Prompt-like nodes

| Node | Parameter | Prompt Evidence |
| --- | --- | --- |
| Send Email via GHL | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send Email via GHL | `jsonBody` | ={<br>  "type": "Email",<br>  "contactId": "{{ $json.contact_id }}",<br>  "emailFrom": "{{ $json.coordinatorEmail }}",<br>  "html": {{ JSON.stringify($json.html) }},<br>  "subject": "{{ $json.subject }}"<br>} |
| Sticky Note - Send & Log | `content` | ## 3. BUILD, SEND & LOG<br>**Build HTML Email** wraps `full_content` in HTML template with unsubscribe footer (Reply STOP).<br>**Send Email via GHL** sends as Email type via conversations/messages API.<br>**Log Delivery** inserts into `newsletter_deliveries` with status=sent + lead_id.<br>After logging, loops back to next agent.<br><br>**To test:** Hit Test Workflow in the editor. Your lead (Hobaid) is opted in with APPT_OFFERED stage = WEEKLY cadence. |

## 🧠 SMRT Brain Engine

| Field | Value |
| --- | --- |
| File | `workflows/active/SMRT_Brain_Engine__mlR5dZuzXxP_JYGaqrqpu.json` |
| Active | `True` |
| Nodes | `174` |
| Edges | `178` |
| Table refs | `{"agents": 4, "documents": 1, "leads": 16, "message_log": 8, "message_send_errors": 1, "splinter_usage": 1}` |
| API refs | `{"leadconnectorhq.com/conversations/messages": 2}` |

### Approximate execution order

`Anthropic Chat Model → Embeddings → RAG LLM → Schedule Outbound Check → Webhook → addAppointmentNotes → bookAppointment → checkQualificationStatus → deleteAppointment → getAppointments → getAvailableSlots → getContact → getNotes → rescheduleAppointment → saveQualifyingAnswer → searchPastMessages → subscribeToNewsletter → switchChannel → updateContactMemory → Docs Store → Business Hours Check → Save Inbound Capture → KB Tool → Apply Time Decay → Check Capture Mode → Fetch Outbound Candidates → Capture Mode On? → Set Outbound Context → End Captured → Restore Webhook Context → Get Outbound Agent Config → Get Outbound Message History → Get Outbound Conversation Summary → Get Outbound Lead Memory → Set Direction: Inbound → Merge Outbound Context → LeadDetails → Get Agent Config → inboundTrue? → whichChannel? → errorLogMessageType → setChannelSMS → setChannelEmail → setChannelInstagram → searchLeads → leadExists? → addLead → carryContactID → L2 Hard Block Gate → L2 Silent Drop → logMessageLeads → Update Inbound Timestamps → Stamp Batch Key → Wait 90s → Check Batch Leader → Am I Batch Leader? → Set Direction: Inbound2 → Get Agent Config (RAG) → Get Message History → Get Conversation Summary → Get Lead Memory → Merge Context → Extract Keywords → Check Keyword Search → Search Relevant Messages → Merge Messages → Merge & Deduplicate Messages → Gather Sentiment Data1 → Prepare Sentiment Context → AI Sentiment Analysis → Parse Sentiment → Turnaround Gate → Turnaround: Clear State → L1 Drop Gate → Turnaround: Disable DND → Route Intent → Turnaround: Remove L1 Tag → Tier Sub-Router1 → Block Level Router → Check Escalation State → …`

### Relevant nodes and parameters

| Node | Type | Parameter | Evidence |
| --- | --- | --- | --- |
| Sticky Note3 | `n8n-nodes-base.stickyNote` | `content` | ## AI Model (RAG System)<br><br>Dynamic context gathering → Prompt assembly → AI response → GHL send |
| LeadDetails | `n8n-nodes-base.set` | `assignments.assignments[6].name` | email |
| LeadDetails | `n8n-nodes-base.set` | `assignments.assignments[6].value` | ={{ $json.body.email }} |
| addLead | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldId` | email |
| addLead | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $('LeadDetails').item.json.email }} |
| addLead | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[11].fieldValue` | ={{ (() => { try { return $('setChannelSMS').item.json.channel; } catch(e) { try { return $('setChannelEmail').item.json.channel; } catch(e2) { return $('LeadDetails').item.json.channel; } } })() }} |
| whichChannel? | `n8n-nodes-base.switch` | `rules.values[0].conditions.conditions[0].rightValue` | SMS |
| whichChannel? | `n8n-nodes-base.switch` | `rules.values[0].outputKey` | SMS |
| whichChannel? | `n8n-nodes-base.switch` | `rules.values[1].conditions.conditions[0].rightValue` | Email |
| whichChannel? | `n8n-nodes-base.switch` | `rules.values[1].outputKey` | EMAIL |
| Get Conversation Summary | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Log Outbound Message | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldId` | ghl_message_id |
| Log Outbound Message | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[8].fieldId` | ghl_conversation_id |
| Log Outbound Message | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[8].fieldValue` | ={{ $json.conversationId }} |
| Log Outbound Message | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[9].fieldId` | ghl_accepted_at |
| AI Sentiment Analysis | `@n8n/n8n-nodes-langchain.anthropic` | `messages.values[0].content` | =CONVERSATION CONTEXT (read first — context frames the current message):<br><br>Conversation Summary: {{ $json.conversationSummary }}<br><br>Recent Messages (newest last):<br>{{ $json.historyText }}<br><br>Lead State:<br>- First Name: {{ $json.firstName }}<br>- Agent: {{ $json.agentName }}<br>- Pipeline Stage: {{ $json.pipelineStage }}<br>- Pipeline State: {{ $json.pipelineState }}<br><br>=== CURRENT MESSAGE (classify using STEP 1 → 4 from system prompt) ===<br>{{ $json.currentMessage }}<br><br>Return ONLY valid JSON. No markdown fences. No e…[TRUNCATED] |
| AI Sentiment Analysis | `@n8n/n8n-nodes-langchain.anthropic` | `options.system` | You are a sentiment classifier for SMRT Bot. You analyze inbound messages and classify them into behavioral tiers to route the Brain Engine correctly.<br><br>CRITICAL: Your reasoning field is used INTERNALLY by downstream nodes ONLY. It is NEVER shown to the lead. The Brain Engine AI Agent uses its own prompt to write the actual SMS/email reply. Be thorough in your reasoning - it helps downstream routing, it never leaks to the customer.<br><br>=== CLASSIFICATION PRIORITY ORDER ===<br><br>Follow these steps IN ORD…[TRUNCATED] |
| Docs Store | `@n8n/n8n-nodes-langchain.vectorStoreSupabase` | `tableName.value` | documents |
| Docs Store | `@n8n/n8n-nodes-langchain.vectorStoreSupabase` | `tableName.cachedResultName` | documents |
| Docs Store | `@n8n/n8n-nodes-langchain.vectorStoreSupabase` | `options.queryName` | match_documents |
| Apply Time Decay | `n8n-nodes-base.postgres` | `query` | UPDATE leads<br>SET<br>  pipeline_state = CASE<br>    WHEN pipeline_state = 'hot'  AND last_customer_message_at < NOW() - INTERVAL '14 days' THEN 'warm'<br>    WHEN pipeline_state = 'warm' AND last_customer_message_at < NOW() - INTERVAL '42 days' THEN 'cold'<br>    ELSE pipeline_state<br>  END<br>WHERE status = 'active_conversation'<br>  AND pipeline_state IN ('hot', 'warm')<br>  AND last_customer_message_at IS NOT NULL<br>  AND location_id = '{{ $json.location_id }}'<br>  AND (<br>    (pipeline_state = 'hot'  AND last_customer_me…[TRUNCATED] |
| Fetch Outbound Candidates | `n8n-nodes-base.postgres` | `query` | WITH eligible_splinter AS (<br>  SELECT<br>    cs.id AS splinter_id,<br>    cs.location_id,<br>    cs.content AS splinter_content,<br>    cs.topic AS splinter_topic,<br>    cs.data_point AS splinter_data_point,<br>    cs.splinter_title,<br>    cs.dominant_theme,<br>    cs.signal_type,<br>    cs.insight_core,<br>    cs.interpretation,<br>    cs.behavioral_implication,<br>    cs.audience_fit,<br>    cs.stage_fit,<br>    cs.embedding_text,<br>    cs.delivery_variants,<br>    cs.priority,<br>    cs.week_start_date,<br>    cs.created_at<br>  FROM content_spli…[TRUNCATED] |
| Prepare Sentiment Context | `n8n-nodes-base.code` | `jsCode` | const d = $input.first().json;<br><br>return [{<br>  json: {<br>    ...d,<br>    currentMessage: d.currentMessage \|\| '',<br>    historyText: d.messageHistory \|\| 'No previous messages',<br>    conversationSummary: d.conversationSummary \|\| 'No summary',<br>    lastIntent: 'none',<br>    agentName: d.agentName \|\| 'Luke Gilbert',<br>    contactId: d.contactId \|\| '',<br>    locationId: d.locationId \|\| '',<br>    firstName: d.firstName \|\| 'Contact',<br>    pipelineStage: d.pipelineStage \|\| 'MONTHLY',<br>    pipelineState: d.pipelineState \|\| '…[TRUNCATED] |
| Sticky Note13 | `n8n-nodes-base.stickyNote` | `content` | ## Pull Memory<br><br>1. Message Logs<br>2. Conversation_summary/context<br><br> |
| Add GHL Tag - Needs Review | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/tags |
| Move GHL Pipeline Stage | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/opportunities/{{ $json.opportunity_id }} |
| Analyze Conversation | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[0].content` | You analyze real estate AI conversation exchanges and extract structured signals.<br><br>Return ONLY a valid JSON object (no markdown, no explanation):<br>{<br>  "summary": "250-350 word accumulative summary",<br>  "lead_intent": "buy\|sell\|both\|unknown",<br>  "lead_timeline": "now\|3_months\|6_months\|1_year\|unknown",<br>  "appointment_discussed": true or false,<br>  "appointment_outcome": "offered\|accepted\|rejected\|rescheduled\|null",<br>  "newsletter_discussed": true or false,<br>  "newsletter_declined": true or false,<br>  "news…[TRUNCATED] |
| Analyze Conversation | `@n8n/n8n-nodes-langchain.openAi` | `messages.values[1].content` | =Previous Summary: {{ (() => { try { return $('Get Outbound Conversation Summary').first().json.conversation_summary } catch(e) { try { return $('Get Conversation Summary').first().json.conversation_summary } catch(e2) { return 'None' } } })() }}<br>Previous Intent: {{ (() => { try { return $('Get Outbound Conversation Summary').first().json.lead_intent } catch(e) { try { return $('Get Conversation Summary').first().json.lead_intent } catch(e2) { return 'unknown' } } })() }}<br>Previous Timeline: {{ (…[TRUNCATED] |
| Update Conversation Context | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | conversation_summary |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ (() => {  const turnCounter = (() => {   try { return parseInt($('searchLeads').first().json.turn_counter) \|\| 0 }   catch(e) {    try { return parseInt($('Get Lead Memory').first().json.turn_counter) \|\| 0 }    catch(e2) {     try { return parseInt($('Get Outbound Lead Memory').first().json.turn_counter) \|\| 0 }     catch(e3) { return 0 }    }   }  })();  const shouldRegen = (turnCounter <= 2 \|\| turnCounter % 10 === 0);  if (shouldRegen) {   try {    const content = $('Analyze Conversation').f…[TRUNCATED] |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.lead_intent \|\| 'unknown'; } catch(e) { return 'unknown'; } })() }} |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[4].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.lead_timeline \|\| 'unknown'; } catch(e) { return 'unknown'; } })() }} |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.appointment_discussed && (parsed.appointment_outcome === 'offered' \|\| parsed.appointment_outcome === 'accepted'); } catch(e) { return false; } })() }} |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.appointment_discussed && parsed.appointment_outcome === 'accepted'; } catch(e) { return false; } })() }} |
| Update Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[7].fieldValue` | ={{ (() => {  const turnCounter = (() => {   try { return parseInt($('searchLeads').first().json.turn_counter) \|\| 0 }   catch(e) {    try { return parseInt($('Get Lead Memory').first().json.turn_counter) \|\| 0 }    catch(e2) { return 0 }   }  })();  const shouldRegen = (turnCounter <= 2 \|\| turnCounter % 10 === 0);  if (shouldRegen) {   return new Date().toISOString();  } else {   try { return $('Get Outbound Conversation Summary').first().json.last_summarized_at \|\| null }   catch(e) { try { retur…[TRUNCATED] |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[0].conditions.conditions[0].id` | route-sms |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[0].conditions.conditions[0].leftValue` | =={{ (() => { const ctx = $('Assemble System Prompt').first().json; if (ctx.direction === 'inbound') return ctx.channel \|\| 'sms'; try { const pref = $('Get Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} try { const pref = $('Get Outbound Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} return ctx.channel \|\| 'sms'; })() }} |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[0].conditions.conditions[0].rightValue` | SMS |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[0].outputKey` | SMS |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[1].conditions.conditions[0].id` | route-email |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[1].conditions.conditions[0].leftValue` | =={{ (() => { const ctx = $('Assemble System Prompt').first().json; if (ctx.direction === 'inbound') return ctx.channel \|\| 'sms'; try { const pref = $('Get Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} try { const pref = $('Get Outbound Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} return ctx.channel \|\| 'sms'; })() }} |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[1].conditions.conditions[0].rightValue` | Email |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[1].outputKey` | Email |
| Route by Channel1 | `n8n-nodes-base.switch` | `rules.values[2].conditions.conditions[0].leftValue` | =={{ (() => { const ctx = $('Assemble System Prompt').first().json; if (ctx.direction === 'inbound') return ctx.channel \|\| 'sms'; try { const pref = $('Get Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} try { const pref = $('Get Outbound Lead Memory').first().json.contact_preference; if (pref && pref !== 'unknown') return pref; } catch(e) {} return ctx.channel \|\| 'sms'; })() }} |
| Send SMS | `n8n-nodes-base.httpRequest` | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send SMS | `n8n-nodes-base.httpRequest` | `jsonBody` | ={{ JSON.stringify({ type: 'SMS', contactId: $('Assemble System Prompt').first().json.contactId, message: $json.responseText \|\| $('AI Agent').first().json.output }) }} |
| Send Email | `n8n-nodes-base.httpRequest` | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send Email | `n8n-nodes-base.httpRequest` | `jsonBody` | ={{ (() => { const raw = $json.responseText \|\| $('AI Agent').first().json.output; const contactId = $('Assemble System Prompt').first().json.contactId; const coordinatorEmail = (() => { try { return $('Get Outbound Agent Config').first().json.coordinator_email } catch(e) { return $('Get Agent Config').first().json.coordinator_email } })(); const coordinatorName = (() => { try { return $('Get Outbound Agent Config').first().json.coordinator_name } catch(e) { try { return $('Get Agent Config').fir…[TRUNCATED] |
| Prepare Tier Response | `n8n-nodes-base.code` | `jsCode` | // Prepare tier-specific response instructions for the AI Agent<br>// Pull tier/sentiment/sms_action from Parse Sentiment directly — input $json may be from Apply L1: Mark Lead (no tier)<br>const ps = (() => { try { return $('Parse Sentiment').first().json; } catch(e) { return {}; } })();<br>const tier = ps.tier \|\| $json.tier \|\| 'normal';<br>const sentiment = ps.sentiment \|\| $json.sentiment \|\| 'neutral';<br>const smsAction = ps.sms_action \|\| $json.sms_action \|\| 'none';<br><br>const tierInstructions = {<br>  tier_1_conf…[TRUNCATED] |
| Pause SMS 30 Days (Tier 2) | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | sms_paused_until |
| Tag Tier 2 in GHL | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $json.contact_id }}/tags |
| Tag Tier 2 in GHL | `n8n-nodes-base.httpRequest` | `jsonBody` | ={"tags": ["SMRT-Tier-2-Hostile", "SMS-Paused-30-Days"]} |
| Search Relevant Messages | `n8n-nodes-base.postgres` | `name/type only` |  |
| Merge Messages | `n8n-nodes-base.merge` | `name/type only` |  |
| Merge & Deduplicate Messages | `n8n-nodes-base.code` | `jsCode` | // Merge recent messages + keyword-matched messages, deduplicate by id<br>const allMessages = $input.all().map(item => item.json);<br><br>// Deduplicate by id (not message_id)<br>const seen = new Set();<br>const uniqueMessages = [];<br><br>for (const msg of allMessages) {<br>  const msgId = msg.id \|\| msg.message_id; // Support both field names<br>  if (!seen.has(msgId)) {<br>    seen.add(msgId);<br>    uniqueMessages.push(msg);<br>  }<br>}<br><br>// Sort by timestamp (newest first)<br>uniqueMessages.sort((a, b) => {<br>  const timeA = new Date(a…[TRUNCATED] |
| Summary Exists? | `n8n-nodes-base.if` | `conditions.conditions[0].leftValue` | ={{ (() => { try { return $('Get Outbound Conversation Summary').first().json.id } catch(e) { try { return $('Get Conversation Summary').first().json.id } catch(e2) { return undefined } } })() }} |
| searchPastMessages | `n8n-nodes-base.supabaseTool` | `toolDescription` | Search this lead's past message log beyond the 15 most recent messages already in your context.<br><br>WHEN TO USE:<br>- Lead references something old ("I told you", "I mentioned before", "remember when", "weeks ago")<br>- You need to verify a claim about a past conversation<br>- You need to recall a commitment, date, or topic from earlier in the relationship<br><br>WHEN NOT TO USE:<br>- For general knowledge or company info (use KB Tool)<br>- For information already visible in RECENT MESSAGES (your 15-turn window)<br><br>INPUT…[TRUNCATED] |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[1].value` | ={{ $json.channel \|\| 'sms' }} |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[6].value` | ={{ (() => { const stage = ($json.pipeline_stage \|\| 'MONTHLY').toLowerCase(); const dv = $json.delivery_variants \|\| {}; const touchpoint = dv[stage + '_touchpoint'] \|\| $json.splinter_content \|\| ''; const firstName = $json.first_name \|\| 'them'; const coordinatorName = $json.coordinator_name \|\| 'a teammate'; const agentName = $json.agent_name \|\| 'the team'; const marketName = $json.market_name \|\| 'the local market'; const pref = ($json.contact_preference \|\| '').toLowerCase(); const channel = (pref…[TRUNCATED] |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[7].name` | splinter_id |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[7].value` | ={{ $json.splinter_id }} |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[8].name` | splinter_topic |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[8].value` | ={{ $json.splinter_topic }} |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[9].name` | splinter_data_point |
| Set Outbound Context | `n8n-nodes-base.set` | `assignments.assignments[9].value` | ={{ $json.splinter_data_point }} |
| Get Outbound Conversation Summary | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Sticky Note15 | `n8n-nodes-base.stickyNote` | `content` | ## Pull Memory<br><br>1. Message Logs<br>2. Conversation_summary/context<br><br> |
| Fetch Outbound Note | `n8n-nodes-base.stickyNote` | `content` | ## Fetch Outbound Candidates<br><br>**What it pulls:**<br>- contact_id, location_id, first_name, channel<br>- pipeline_state (hot / warm / cold)<br>- agent_name, market_name<br>- Most recent active splinter per agent (splinter_id, splinter_content, splinter_topic, splinter_data_point)<br><br>**Conditions:**<br>- status = 'active_conversation'<br>- pipeline_stage != 'DNC'<br>- agent_dormant = false<br>- sms_paused_until IS NULL or has expired<br>- next_outbound_due_at IS NULL or has passed<br><br>**Ordered by:** next_outbound_due_at ASC (ol…[TRUNCATED] |
| Sticky Note16 | `n8n-nodes-base.stickyNote` | `content` | ## Gohighlevel Tools<br><br><br> |
| getContact | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `toolDescription` | Fetch the full contact profile for the current lead from GoHighLevel, including custom fields, tags, lead source, and all contact details.<br><br>WHEN TO USE:<br>- When you need lead details NOT available in the conversation (email, address, tags, lead source)<br>- When the lead asks you to confirm their contact information<br>- When you need CRM context about the lead's history or status<br><br>INPUT: No input needed. Automatically looks up the current lead.<br><br>OUTPUT: Full contact record with name, email, phone, tag…[TRUNCATED] |
| getContact | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ (() => { try { return $('Assemble System Prompt').first().json.contactId } catch(e) { return $('LeadDetails').first().json.contact_id } })() }} |
| getNotes | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `toolDescription` | Fetch all notes recorded on this lead's GoHighLevel contact by agents or the AI system.<br><br>WHEN TO USE:<br>- When the lead references a previous conversation or commitment ("someone told me...", "I was promised...")<br>- When you need historical context about what has been discussed or agreed with this lead<br>- Before escalating, to check if there are existing agent notes<br><br>INPUT: No input needed. Automatically looks up the current lead.<br><br>OUTPUT: List of notes with timestamps and content.<br><br>RULES:<br>- Use to …[TRUNCATED] |
| getNotes | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ (() => { try { return $('Assemble System Prompt').first().json.contactId } catch(e) { return $('LeadDetails').first().json.contact_id } })() }}/notes |
| getAppointments | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `toolDescription` | Fetch all appointments (past and upcoming) for the current lead from GoHighLevel.<br><br>WHEN TO USE:<br>- When the lead asks about existing appointments ("when is my appointment?", "do I have anything scheduled?")<br>- When the lead wants to cancel or reschedule (you need the event_id from here)<br>- When you need to check if the lead already has a booking before offering to schedule<br><br>INPUT: No input needed. Automatically looks up the current lead.<br><br>OUTPUT: List of appointment objects with event IDs, dates, t…[TRUNCATED] |
| getAppointments | `@n8n/n8n-nodes-langchain.toolHttpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ (() => { try { return $('Assemble System Prompt').first().json.contactId } catch(e) { return $('LeadDetails').first().json.contact_id } })() }}/appointments |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldId` | conversation_summary |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.summary \|\| content; } catch(e) { return $('Analyze Conversation').first().json.message?.content \|\| ''; } })() }} |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[4].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| '{}'; const parsed = JSON.parse(content); return parsed.lead_intent \|\| 'unknown'; } catch(e) { return 'unknown'; } })() }} |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[5].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| '{}'; const parsed = JSON.parse(content); return parsed.lead_timeline \|\| 'unknown'; } catch(e) { return 'unknown'; } })() }} |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[8].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| '{}'; const parsed = JSON.parse(content); return parsed.appointment_discussed && (parsed.appointment_outcome === 'offered' \|\| parsed.appointment_outcome === 'accepted'); } catch(e) { return false; } })() }} |
| Insert Conversation Context | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[9].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| '{}'; const parsed = JSON.parse(content); return parsed.appointment_discussed && parsed.appointment_outcome === 'accepted'; } catch(e) { return false; } })() }} |
| addAppointmentNotes | `@n8n/n8n-nodes-langchain.toolCode` | `jsonSchemaExample` | {"appointment_time":"2026-04-10T09:00","qualifying_summary":"Buyer, 500k, pre-approved","conversation_summary":"Lead relocating"} |
| Form Section4 | `n8n-nodes-base.stickyNote` | `content` | ##  Booking Agent Routing<br>#### Book Intent in conversation<br> |
| checkQualificationStatus | `n8n-nodes-base.supabaseTool` | `toolDescription` | Check how many of the 3 qualifying questions this lead has answered so far.<br><br>WHEN TO USE:<br>- BEFORE calling saveQualifyingAnswer, to get the current qualifying_answers object<br>- When the lead says they want to book an appointment, to verify they are qualified<br>- When you are unsure which qualifying questions have already been answered<br><br>INPUT: No input needed. Automatically looks up this lead's conversation_context record.<br><br>OUTPUT: Returns the qualifying_answers JSONB object with keys q1, q2, q3.<br>- …[TRUNCATED] |
| checkQualificationStatus | `n8n-nodes-base.supabaseTool` | `tableId` | conversation_context |
| saveQualifyingAnswer | `n8n-nodes-base.supabaseTool` | `toolDescription` | MANDATORY TOOL - Save a qualifying answer to the database after the lead answers one of the 3 qualifying questions.<br><br>WHEN TO USE: You MUST call this tool IMMEDIATELY every time the lead provides information that answers Q1, Q2, or Q3. Do NOT wait until all questions are answered. Save each answer the moment you receive it.<br><br>The tool updates the qualifying_answers JSONB column in conversation_context for this lead.<br><br>INPUT: You must provide a JSON object with the field 'qualifying_answers' contain…[TRUNCATED] |
| saveQualifyingAnswer | `n8n-nodes-base.supabaseTool` | `tableId` | conversation_context |
| Check Qualification Gate | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Sync Fields to GHL | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('Build GHL Sync Body').first().json.contactId }} |
| Sync Fields to GHL | `n8n-nodes-base.httpRequest` | `jsonBody` | ={{ JSON.stringify(Object.assign({}, $('Build GHL Sync Body').first().json.coreFields, { customFields: $('Build GHL Sync Body').first().json.customFields })) }} |
| Post Memory Note to GHL | `n8n-nodes-base.code` | `name/type only` |  |
| Get GHL Contact | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/locations/{{ $('Get Lead Memory (After)').first().json.location_id }}/customFields |
| Build GHL Sync Body | `n8n-nodes-base.code` | `jsCode` | // Get GHL custom field definitions from input (Get GHL Contact)<br>const ghlFields = $input.first().json.customFields \|\| [];<br><br>// Get memory + enrichment values from Compare Memory<br>const mem = $('Compare Memory').first().json;<br><br>// === CORE FIELDS (standard GHL contact fields) ===<br>const coreFields = {};<br>if (mem.first_name) coreFields.firstName = mem.first_name;<br>if (mem.last_name) coreFields.lastName = mem.last_name;<br>if (mem.email) coreFields.email = mem.email;<br>if (mem.phone) coreFields.phone = mem.p…[TRUNCATED] |
| Update Inbound Timestamps | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldValue` | ={{ $('LeadDetails').first().json.channel.toLowerCase().includes('sms') ? 'sms' : $('LeadDetails').first().json.channel.toLowerCase().includes('email') ? 'email' : $('LeadDetails').first().json.channel.toLowerCase().includes('ig') ? 'instagram' : 'sms' }} |
| Move Lead in GHL | `n8n-nodes-base.code` | `name/type only` |  |
| updateContactMemory | `@n8n/n8n-nodes-langchain.toolCode` | `description` | Update the contact's memory and enrichment fields. Call this when you detect ANY of the signals below. Include ALL detected fields in a single call — never make multiple calls.<br><br>INPUT (required JSON):<br>- contact_id: string - CONTACT_ID from context (always required)<br>- Plus any fields below that you detected:<br><br>FIELD TRIGGERS — scan every message for these:<br><br>IDENTITY / CONTACT INFO<br>- Lead says their name → first_name, last_name<br>- Lead gives their email address → email<br>- Lead gives their phone numbe…[TRUNCATED] |
| updateContactMemory | `@n8n/n8n-nodes-langchain.toolCode` | `jsonSchemaExample` | {"contact_id":"abc123","first_name":"Mike","last_name":"Johnson","email":"mike@test.com","phone":"208-555-1234","address":"123 Oak St","city":"Eagle","state":"Idaho","postal_code":"83616","market_role":"buyer","intent_topic":"buying","intent_level":"active","contact_preference":"sms","relationship_type":"sphere","open_loop":"needs to sell CA house before buying, target June","handoff_reason":"wants legal advice on lease","needs_human_review":true,"review_reason":"recent bereavement, pause outrea…[TRUNCATED] |
| Notify Agent | `n8n-nodes-base.code` | `jsCode` | const input = $input.first().json;<br><br>const agentEmail = input.agent_email \|\| null;<br>const agentPhone = input.agent_phone \|\| null;<br>const agentName = input.agent_name \|\| input.agentName \|\| 'Agent';<br>const firstName = input.first_name \|\| input.firstName \|\| 'A lead';<br>const message = input.message \|\| input.message_body \|\| '';<br>const responseText = input.responseText \|\| input.output \|\| '';<br>const contactId = input.contact_id \|\| input.contactId \|\| '';<br><br>const summary = firstName + ' needs ' + agentName + "\'…[TRUNCATED] |
| setChannelSMS | `n8n-nodes-base.code` | `jsCode` | const items = $input.all();<br>return items.map(item => ({<br>  json: { ...item.json, channel: 'SMS' }<br>})); |
| setChannelEmail | `n8n-nodes-base.code` | `jsCode` | const items = $input.all();<br>return items.map(item => ({<br>  json: { ...item.json, channel: 'Email' }<br>})); |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[0].id` | newsletter-check-appt-rejected |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[0].leftValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| '{}'; const parsed = JSON.parse(content); return parsed.appointment_discussed === true && parsed.appointment_outcome === 'rejected'; } catch(e) { return false; } })() }} |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[1].id` | newsletter-check-not-opted-in |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[1].leftValue` | ={{ (() => { try { return $('Get Lead Memory').first().json.newsletter_opted_in } catch(e) { try { return $('Get Outbound Lead Memory').first().json.newsletter_opted_in } catch(e2) { return false } } })() }} |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[2].id` | newsletter-check-not-declined |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[2].leftValue` | ={{ (() => { try { return $('Get Conversation Summary').first().json.newsletter_offer_declined } catch(e) { try { return $('Get Outbound Conversation Summary').first().json.newsletter_offer_declined } catch(e2) { return false } } })() }} |
| Newsletter Offer Needed? | `n8n-nodes-base.if` | `conditions.conditions[3].id` | newsletter-check-not-outbound |
| Set Newsletter Pending | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Set Newsletter Pending | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | newsletter_offer_pending |
| Clear Newsletter Flags | `n8n-nodes-base.supabase` | `tableId` | conversation_context |
| Clear Newsletter Flags | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | newsletter_offer_pending |
| Clear Newsletter Flags | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldId` | newsletter_offer_declined |
| Clear Newsletter Flags | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[1].fieldValue` | ={{ (() => { try { const leadMemory = $('Get Lead Memory (After)').first()?.json \|\| $('Get Outbound Lead Memory').first()?.json \|\| {}; if (leadMemory.newsletter_opted_in === true) return false; const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.newsletter_declined === true; } catch(e) { return false; } })() }} |
| subscribeToNewsletter | `@n8n/n8n-nodes-langchain.toolCode` | `name` | subscribeToNewsletter |
| subscribeToNewsletter | `@n8n/n8n-nodes-langchain.toolCode` | `description` | Subscribe the current lead to the weekly market newsletter. Updates leads.newsletter_opted_in = true, tags the contact in GHL, and clears pending flags.<br><br>INPUT (required JSON):<br>- contact_id: string - the CONTACT_ID from the context header (always required)<br><br>CALL THIS TOOL when the lead signals they want market updates, want to stay in the loop, accept the newsletter offer, or agree to receive periodic info. Call it BEFORE you write your response acknowledging the subscription — never acknowledge…[TRUNCATED] |
| Record Splinter Usage | `n8n-nodes-base.supabase` | `tableId` | splinter_usage |
| Record Splinter Usage | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldId` | splinter_id |
| Record Splinter Usage | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[0].fieldValue` | ={{ $json.splinter_id }} |
| Record Splinter Usage | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldValue` | ={{ $json.channel \|\| 'sms' }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[2].name` | marketName |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[2].value` | ={{ (() => { try { return $('Get Outbound Agent Config').first().json.market_name; } catch(e) { try { return $('Get Agent Config (RAG)').first().json.market_name; } catch(e2) { return ''; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[5].value` | ={{ (() => { try { return $('Set Outbound Context').first().json.channel; } catch(e) { try { return $('LeadDetails').first().json.channel; } catch(e2) { return 'sms'; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[13].name` | conversationSummary |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[13].value` | ={{ (() => { try { return $('Get Outbound Conversation Summary').first().json.conversation_summary; } catch(e) { try { return $('Get Conversation Summary').first().json.conversation_summary; } catch(e2) { return ''; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[15].name` | marketRole |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[15].value` | ={{ (() => { try { return $('Get Outbound Lead Memory').first().json.market_role; } catch(e) { try { return $('Get Lead Memory').first().json.market_role; } catch(e2) { return 'unknown'; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[20].name` | newsletterPending |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[20].value` | ={{ (() => { try { return $('Get Outbound Conversation Summary').first().json.newsletter_offer_pending \|\| false; } catch(e) { try { return $('Get Conversation Summary').first().json.newsletter_offer_pending \|\| false; } catch(e2) { return false; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[21].name` | newsletterOptedIn |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[21].value` | ={{ (() => { try { return $('Get Outbound Lead Memory').first().json.newsletter_opted_in \|\| false; } catch(e) { try { return $('Get Lead Memory').first().json.newsletter_opted_in \|\| false; } catch(e2) { return false; } } })() }} |
| Gather Prompt Data | `n8n-nodes-base.set` | `assignments.assignments[22].name` | newsletterDeclined |
| Gather Sentiment Data1 | `n8n-nodes-base.set` | `assignments.assignments[4].name` | conversationSummary |
| Gather Sentiment Data1 | `n8n-nodes-base.set` | `assignments.assignments[4].value` | ={{ (() => { try { return $('Get Conversation Summary').first().json.conversation_summary \|\| ''; } catch(e) { try { return $('Get Outbound Conversation Summary').first().json.conversation_summary \|\| ''; } catch(e2) { return ''; } } })() }} |
| Gather Sentiment Data1 | `n8n-nodes-base.set` | `assignments.assignments[6].value` | ={{ (() => { try { const msgs = $('Get Message History').all(); return msgs.slice(0, 15).map(m => (m.json.direction === 'inbound' ? '[lead] ' : '[bot] ') + m.json.message_body).join('\n'); } catch(e) { return 'No previous messages'; } })() }} |
| Has Splinter Data? | `n8n-nodes-base.if` | `conditions.conditions[0].id` | splinter-check |
| Has Splinter Data? | `n8n-nodes-base.if` | `conditions.conditions[0].leftValue` | ={{ $json.splinter_id }} |
| switchChannel | `@n8n/n8n-nodes-langchain.toolCode` | `description` | Switch the lead's communication channel and send a hardcoded bridge message on the new channel. Call this ONLY when the lead explicitly asks to move to a different channel (e.g. "just email me", "text me instead").<br><br>SUPPORTED DESTINATIONS: sms, email. You CANNOT switch TO Instagram DM — only FROM it. If a lead asks to be contacted on Instagram, politely decline and offer SMS or email instead.<br><br>INPUT (required JSON):<br>- contact_id: string — CONTACT_ID from context<br>- new_channel: string — must be "…[TRUNCATED] |
| switchChannel | `@n8n/n8n-nodes-langchain.toolCode` | `jsonSchemaExample` | {"contact_id":"abc123","new_channel":"email"} |
| Sticky Note - Wait Window | `n8n-nodes-base.stickyNote` | `content` | ## Wait Window — Message Batching<br><br>**60s debounce + batch leader election.**<br><br>Stamp log ID → wait 60s → query message_log for last 90s of inbound messages from this contact → newest message (by timestamp, id tiebreaker) is the leader.<br><br>- TRUE (leader): proceeds through pipeline with `batchedMessageBody` (all messages joined)<br>- FALSE (not leader): dead-ends silently — another execution will respond<br><br>If query fails, node fail-safes to leader with single message body (so we never drop a response). |
| Turnaround: Clear State | `n8n-nodes-base.postgres` | `query` | =UPDATE leads SET opt_out_level=NULL, opted_out_at=NULL, opt_out_reason=NULL, sms_paused_until=NULL, status='active_conversation' WHERE contact_id='{{ $('LeadDetails').first().json.contact_id }}' |
| Turnaround: Disable DND | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }} |
| Turnaround: Disable DND | `n8n-nodes-base.httpRequest` | `jsonBody` | { "dnd": false, "dndSettings": { "Call": { "status": "inactive" }, "Email": { "status": "inactive" }, "SMS": { "status": "inactive" }, "WhatsApp": { "status": "inactive" }, "GMB": { "status": "inactive" }, "FB": { "status": "inactive" } } } |
| Turnaround: Remove L1 Tag | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/tags |
| Turnaround: Post Note | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/notes |
| Apply L1: Enable DND | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }} |
| Apply L1: Enable DND | `n8n-nodes-base.httpRequest` | `jsonBody` | { "dnd": true, "dndSettings": { "Call": { "status": "active" }, "Email": { "status": "active" }, "SMS": { "status": "active" }, "WhatsApp": { "status": "active" }, "GMB": { "status": "active" }, "FB": { "status": "active" } } } |
| Apply L1: Add L1 Tag | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/tags |
| Apply L1: Post Note | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/notes |
| Apply L2: Enable DND | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }} |
| Apply L2: Enable DND | `n8n-nodes-base.httpRequest` | `jsonBody` | { "dnd": true, "dndSettings": { "Call": { "status": "active" }, "Email": { "status": "active" }, "SMS": { "status": "active" }, "WhatsApp": { "status": "active" }, "GMB": { "status": "active" }, "FB": { "status": "active" } } } |
| Apply L2: Add L2 Tag | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/tags |
| Apply L2: Post Note | `n8n-nodes-base.httpRequest` | `url` | =https://services.leadconnectorhq.com/contacts/{{ $('LeadDetails').first().json.contact_id }}/notes |
| Newsletter Guard | `n8n-nodes-base.code` | `jsCode` | // Always guarantee 1 item with a stable contact_id so Clear Newsletter Flags never receives 0 items<br>const ctx = $('Assemble System Prompt').first().json;<br>return [{ json: { contactId: ctx.contactId } }]; |
| Leak Detected? | `n8n-nodes-base.if` | `conditions.conditions[0].leftValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.internal_reasoning_leak_detected === true; } catch(e) { return false; } })() }} |
| Log AI Leak | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[6].fieldValue` | ={{ (() => { try { const content = $('Analyze Conversation').first().json.message?.content \|\| $('Analyze Conversation').first().json.text \|\| '{}'; const parsed = JSON.parse(content); return parsed.leak_phrase \|\| ''; } catch(e) { return 'parse_error'; } })() }} |
| Save Inbound Capture | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldId` | ghl_message_id |
| Prep Status Check Input | `n8n-nodes-base.set` | `assignments.assignments[0].value` | ={{ $json.ghl_message_id }} |
| Log Cap Suppression | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[2].fieldId` | ghl_error_code |
| Log Cap Suppression | `n8n-nodes-base.supabase` | `fieldsUi.fieldValues[3].fieldId` | ghl_error_name |

### Prompt-like nodes

| Node | Parameter | Prompt Evidence |
| --- | --- | --- |
| AI Sentiment Analysis | `messages.values[0].content` | =CONVERSATION CONTEXT (read first — context frames the current message):<br><br>Conversation Summary: {{ $json.conversationSummary }}<br><br>Recent Messages (newest last):<br>{{ $json.historyText }}<br><br>Lead State:<br>- First Name: {{ $json.firstName }}<br>- Agent: {{ $json.agentName }}<br>- Pipeline Stage: {{ $json.pipelineStage }}<br>- Pipeline State: {{ $json.pipelineState }}<br><br>=== CURRENT MESSAGE (classify using STEP 1 → 4 from system prompt) ===<br>{{ $json.currentMessage }}<br><br>Return ONLY valid JSON. No markdown fences. No e…[TRUNCATED] |
| AI Sentiment Analysis | `options.system` | You are a sentiment classifier for SMRT Bot. You analyze inbound messages and classify them into behavioral tiers to route the Brain Engine correctly.<br><br>CRITICAL: Your reasoning field is used INTERNALLY by downstream nodes ONLY. It is NEVER shown to the lead. The Brain Engine AI Agent uses its own prompt to write the actual SMS/email reply. Be thorough in your reasoning - it helps downstream routing, it never leaks to the customer.<br><br>=== CLASSIFICATION PRIORITY ORDER ===<br><br>Follow these steps IN ORD…[TRUNCATED] |
| Prepare Sentiment Context | `jsCode` | const d = $input.first().json;<br><br>return [{<br>  json: {<br>    ...d,<br>    currentMessage: d.currentMessage \|\| '',<br>    historyText: d.messageHistory \|\| 'No previous messages',<br>    conversationSummary: d.conversationSummary \|\| 'No summary',<br>    lastIntent: 'none',<br>    agentName: d.agentName \|\| 'Luke Gilbert',<br>    contactId: d.contactId \|\| '',<br>    locationId: d.locationId \|\| '',<br>    firstName: d.firstName \|\| 'Contact',<br>    pipelineStage: d.pipelineStage \|\| 'MONTHLY',<br>    pipelineState: d.pipelineState \|\| '…[TRUNCATED] |
| Analyze Conversation | `messages.values[0].content` | You analyze real estate AI conversation exchanges and extract structured signals.<br><br>Return ONLY a valid JSON object (no markdown, no explanation):<br>{<br>  "summary": "250-350 word accumulative summary",<br>  "lead_intent": "buy\|sell\|both\|unknown",<br>  "lead_timeline": "now\|3_months\|6_months\|1_year\|unknown",<br>  "appointment_discussed": true or false,<br>  "appointment_outcome": "offered\|accepted\|rejected\|rescheduled\|null",<br>  "newsletter_discussed": true or false,<br>  "newsletter_declined": true or false,<br>  "news…[TRUNCATED] |
| Analyze Conversation | `messages.values[1].content` | =Previous Summary: {{ (() => { try { return $('Get Outbound Conversation Summary').first().json.conversation_summary } catch(e) { try { return $('Get Conversation Summary').first().json.conversation_summary } catch(e2) { return 'None' } } })() }}<br>Previous Intent: {{ (() => { try { return $('Get Outbound Conversation Summary').first().json.lead_intent } catch(e) { try { return $('Get Conversation Summary').first().json.lead_intent } catch(e2) { return 'unknown' } } })() }}<br>Previous Timeline: {{ (…[TRUNCATED] |
| Send SMS | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send SMS | `jsonBody` | ={{ JSON.stringify({ type: 'SMS', contactId: $('Assemble System Prompt').first().json.contactId, message: $json.responseText \|\| $('AI Agent').first().json.output }) }} |
| Send Email | `url` | https://services.leadconnectorhq.com/conversations/messages |
| Send Email | `jsonBody` | ={{ (() => { const raw = $json.responseText \|\| $('AI Agent').first().json.output; const contactId = $('Assemble System Prompt').first().json.contactId; const coordinatorEmail = (() => { try { return $('Get Outbound Agent Config').first().json.coordinator_email } catch(e) { return $('Get Agent Config').first().json.coordinator_email } })(); const coordinatorName = (() => { try { return $('Get Outbound Agent Config').first().json.coordinator_name } catch(e) { try { return $('Get Agent Config').fir…[TRUNCATED] |
| Merge & Deduplicate Messages | `jsCode` | // Merge recent messages + keyword-matched messages, deduplicate by id<br>const allMessages = $input.all().map(item => item.json);<br><br>// Deduplicate by id (not message_id)<br>const seen = new Set();<br>const uniqueMessages = [];<br><br>for (const msg of allMessages) {<br>  const msgId = msg.id \|\| msg.message_id; // Support both field names<br>  if (!seen.has(msgId)) {<br>    seen.add(msgId);<br>    uniqueMessages.push(msg);<br>  }<br>}<br><br>// Sort by timestamp (newest first)<br>uniqueMessages.sort((a, b) => {<br>  const timeA = new Date(a…[TRUNCATED] |
| searchPastMessages | `toolDescription` | Search this lead's past message log beyond the 15 most recent messages already in your context.<br><br>WHEN TO USE:<br>- Lead references something old ("I told you", "I mentioned before", "remember when", "weeks ago")<br>- You need to verify a claim about a past conversation<br>- You need to recall a commitment, date, or topic from earlier in the relationship<br><br>WHEN NOT TO USE:<br>- For general knowledge or company info (use KB Tool)<br>- For information already visible in RECENT MESSAGES (your 15-turn window)<br><br>INPUT…[TRUNCATED] |
| getNotes | `toolDescription` | Fetch all notes recorded on this lead's GoHighLevel contact by agents or the AI system.<br><br>WHEN TO USE:<br>- When the lead references a previous conversation or commitment ("someone told me...", "I was promised...")<br>- When you need historical context about what has been discussed or agreed with this lead<br>- Before escalating, to check if there are existing agent notes<br><br>INPUT: No input needed. Automatically looks up the current lead.<br><br>OUTPUT: List of notes with timestamps and content.<br><br>RULES:<br>- Use to …[TRUNCATED] |
| getNotes | `url` | =https://services.leadconnectorhq.com/contacts/{{ (() => { try { return $('Assemble System Prompt').first().json.contactId } catch(e) { return $('LeadDetails').first().json.contact_id } })() }}/notes |
| Gather Sentiment Data1 | `assignments.assignments[4].name` | conversationSummary |
| Gather Sentiment Data1 | `assignments.assignments[4].value` | ={{ (() => { try { return $('Get Conversation Summary').first().json.conversation_summary \|\| ''; } catch(e) { try { return $('Get Outbound Conversation Summary').first().json.conversation_summary \|\| ''; } catch(e2) { return ''; } } })() }} |
| Gather Sentiment Data1 | `assignments.assignments[6].value` | ={{ (() => { try { const msgs = $('Get Message History').all(); return msgs.slice(0, 15).map(m => (m.json.direction === 'inbound' ? '[lead] ' : '[bot] ') + m.json.message_body).join('\n'); } catch(e) { return 'No previous messages'; } })() }} |
| Sticky Note - Wait Window | `content` | ## Wait Window — Message Batching<br><br>**60s debounce + batch leader election.**<br><br>Stamp log ID → wait 60s → query message_log for last 90s of inbound messages from this contact → newest message (by timestamp, id tiebreaker) is the leader.<br><br>- TRUE (leader): proceeds through pipeline with `batchedMessageBody` (all messages joined)<br>- FALSE (not leader): dead-ends silently — another execution will respond<br><br>If query fails, node fail-safes to leader with single message body (so we never drop a response). |

